<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.redis1.mapper.MemberMapper">
    <insert id="insert">
        insert into tbl_member(member_email, member_password, member_name)
        values (#{memberEmail}, #{memberPassword}, #{memberName})
    </insert>

    <select id="selectByMemberEmailAndMemberPassword" resultType="MemberDTO">
        select id, member_email, member_password, member_name
        from tbl_member
    </select>
</mapper>